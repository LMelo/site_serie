<h1>Listagem de Gêneros</h1>

<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Nome</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @generos.each do |genero| %>
      <tr>
        <td><%= genero.gen_nome %></td>
        <td><%= link_to 'Show', genero, class: "btn btn-xs btn-default" %></td>
        <td><%= link_to 'Edit', edit_genero_path(genero), class: "btn btn-xs btn-default" %></td>
        <td><%= link_to 'Destroy', genero, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-xs btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Genero', new_genero_path, class: "btn btn-primary" %>
